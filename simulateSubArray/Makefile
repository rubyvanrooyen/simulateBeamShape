clean:
	rm -f *.log *.last *.loc
	rm -f *.pyc
	rm -f log-simms.txt

realclean: clean
	rm -rf *.flux *.image *.model *.psf *.residual
	rm -f *.enu *.cfg

clobber: realclean
	rm -f *.png
	rm -f *.pdf
	rm -rf *.ms *.MS

# -fin-

